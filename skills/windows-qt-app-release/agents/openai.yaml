interface:
  display_name: "Windows Qt App Release"
  short_description: "Build, polish, package, and publish Windows Qt apps."
  default_prompt: "Use $windows-qt-app-release to build, optimize, package, and publish a Windows Qt desktop app."
